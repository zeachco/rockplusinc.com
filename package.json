{
  "name": "rockplusinc.com",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "prebuild": "npm run sass",
    "prestart": "npm run sass",
    "postinstall": "npm run sass",
    "sass": "sass src/styles/theme.scss:src/styles/style.css",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "fly default"
  },
  "proxy": "http://localhost:8080",
  "devDependencies": {
    "flightplan": "^0.6.14",
    "node-sass": "^3.9.0",
    "react-scripts": "0.4.1"
  },
  "dependencies": {
    "axios": "^0.14.0",
    "quantities": "0.0.0",
    "react": "^15.3.1",
    "react-dom": "^15.3.1",
    "react-redux": "^4.4.5",
    "react-router": "^2.7.0",
    "react-skylight": "^0.4.1",
    "redux": "^3.6.0"
  },
  "eslintConfig": {
    "extends": "./node_modules/react-scripts/config/eslint.js"
  }
}
